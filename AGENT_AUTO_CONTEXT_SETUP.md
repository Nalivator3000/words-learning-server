# 🤖 Автоматическое ознакомление агентов с контекстом

## ✅ Что настроено

Теперь при каждом запуске нового агента Claude Code автоматически получает весь необходимый контекст проекта.

---

## 📋 Компоненты системы

### 1. **`.claude/settings.json`** - Основная конфигурация
- ✅ `alwaysReadProjectRules: true` - автоматически читает `.clinerules` при старте
- ✅ `customInstructions` - встроенная инструкция с ключевыми правилами и фактами
- ✅ Содержит критические правила (NEVER push to main, NEVER test locally)
- ✅ Quick facts о проекте (версия, архитектура, URL)
- ✅ Стандартный workflow для типичных задач
- ✅ Команды для тестирования и деплоя

### 2. **`.clinerules`** - Детальные правила (390 строк)
- ⛔ Абсолютные правила (git branch policy, testing environment)
- 🏗️ Архитектура проекта (stack, database schema)
- 🔌 API endpoints и паттерны кода
- 🧪 Тестирование (команды, test accounts)
- 📦 Deployment процесс (Railway)
- 🔒 Security & Performance

### 3. **`AGENT_CONTEXT.md`** - Быстрая справка
- 📊 Project status dashboard
- ⚡ Quick commands reference
- 🚨 Critical rules summary
- 🏗️ Architecture overview
- 📂 Important files locations
- 🌐 URLs and access info

### 4. **`scripts/check-agent-context.js`** - Скрипт проверки
- 📦 Git status and recent commits
- 🔢 Project version
- 🗄️ Database connection status
- 📁 Critical files check
- 🛡️ Branch protection status
- ⚡ Quick commands reference

---

## 🔄 Как это работает

### При запуске нового агента:

1. **Автоматически** (через `alwaysReadProjectRules`):
   - Читается `.clinerules` (390 строк детальных правил)

2. **В `customInstructions`** агент сразу видит:
   - ⛔ CRITICAL RULES (zero tolerance)
   - 📊 PROJECT QUICK FACTS
   - 🗄️ DATABASE ARCHITECTURE
   - ⚡ STANDARD WORKFLOW
   - 🧪 TESTING COMMANDS
   - 📋 COMMON TASKS

3. **Агент получает напоминание**:
   - "Read AGENT_CONTEXT.md for quick project overview"
   - "Check git branch before operations"
   - "Verify version in package.json"

### Опционально агент может:
- Прочитать `AGENT_CONTEXT.md` для быстрого обзора
- Запустить `node scripts/check-agent-context.js` для полной проверки контекста

---

## 🎯 Что агент знает сразу при старте

### ⛔ Критические правила
```
RULE #0:     NEVER push to 'main' → ONLY 'develop'
RULE #0.5:   NEVER test locally → ONLY Railway production
RULE #1:     All DB operations require Railway environment
RULE #2:     Never commit sensitive data (.env, credentials)
RULE #3:     Use Read/Edit/Write tools (NOT bash cat/sed)
```

### 📊 Факты о проекте
- **Project**: LexyBooster - Multi-Language Learning Platform
- **Version**: 5.4.15 (автоматически обновляется)
- **Stack**: Node.js + Express + PostgreSQL
- **Deployment**: Railway (auto-deploy from develop)
- **Languages**: 18 (German, English, Spanish, etc.)
- **Test accounts**: 3 accounts in TEST_ACCOUNTS_READY.md

### 🏗️ Архитектура
- `source_words_{language}` - Shared vocabularies
- `user_word_progress` - Individual progress tracking
- `word_sets` - 170+ thematic collections
- Language pairs: `native→learning` (e.g., `ru→de`)

### ⚡ Стандартный workflow
```bash
git add .
git commit -m "feat: description"
git push origin develop  # ← CRITICAL: Never main!
```

---

## 🚀 Команды для быстрой проверки

### Полная проверка контекста
```bash
node scripts/check-agent-context.js
```

### Быстрая проверка статуса
```bash
git status && git branch && git log -3 --oneline
```

### Проверка версии
```bash
node -p "require('./package.json').version"
```

---

## 📖 Иерархия документации

```
Уровень 1 (Авто-чтение при старте):
├── .clinerules (390 строк) - Детальные правила
└── customInstructions в settings.json - Ключевые факты

Уровень 2 (Быстрая справка):
├── AGENT_CONTEXT.md - Structured overview
└── scripts/check-agent-context.js - Automated check

Уровень 3 (Специфичная документация):
├── TEST_ACCOUNTS_READY.md - Test credentials
├── TESTING_QUICK_START.md - Testing guide
├── RAILWAY_QUICK_START.md - Deployment guide
└── package.json - Scripts and dependencies
```

---

## 🔧 Обслуживание

### Когда обновлять?

**`customInstructions` в settings.json:**
- ✅ При изменении версии проекта
- ✅ При изменении критических правил
- ✅ При смене основного branch
- ✅ При смене production URL

**`AGENT_CONTEXT.md`:**
- ✅ При добавлении новых языков
- ✅ При изменении количества word sets
- ✅ При обновлении архитектуры БД
- ✅ При добавлении новых команд

**`.clinerules`:**
- ✅ При изменении workflow
- ✅ При добавлении новых API endpoints
- ✅ При изменении deployment процесса
- ✅ При обновлении security правил

**`scripts/check-agent-context.js`:**
- ✅ При добавлении новых critical files
- ✅ При изменении проверок статуса

---

## 💡 Преимущества

✅ **Агент всегда в контексте** - не нужно каждый раз объяснять правила
✅ **Защита от ошибок** - критические правила видны сразу
✅ **Быстрый старт** - агент знает команды и workflow
✅ **Консистентность** - все агенты следуют одним правилам
✅ **Экономия времени** - не нужно повторять базовую информацию
✅ **Безопасность** - явные запреты на опасные операции

---

## 🎓 Для пользователя

Теперь при запуске нового агента вы можете сразу давать задачи, не объясняя:
- ❌ Не нужно напоминать "пушь в develop"
- ❌ Не нужно говорить "тестируй на Railway"
- ❌ Не нужно объяснять архитектуру БД
- ❌ Не нужно перечислять команды

Агент **уже знает** все это автоматически! 🎉

---

## 🧪 Проверка работы

При следующем запуске агента просто спросите:
```
"Какие критические правила этого проекта?"
```

Агент должен ответить про:
- NEVER push to main (only develop)
- NEVER test locally (only Railway)
- Database operations require Railway
- No sensitive data in commits
- Use Read/Edit/Write tools

---

**Дата создания**: 2026-01-06
**Версия документа**: 1.0
**Статус**: ✅ Активно и настроено
