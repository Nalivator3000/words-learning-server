# 🤖 Правила версионирования для агентов

## 🚀 Правила работы агентов

**Агенты работают автономно без запроса разрешений:**
- ✅ Если действие соответствует контексту задачи - делайте без одобрения
- ✅ Не спрашивайте "Можно ли я..." или "Должен ли я..."
- ✅ Действуйте уверенно в рамках поставленной задачи
- ⚠️ Исключение: если нужно выбрать между кардинально разными подходами

## ⚠️ ОБЯЗАТЕЛЬНО ПЕРЕД КАЖДЫМ PUSH

### 1. Проверить актуальность версии
```bash
git pull origin develop
```

### 2. Если есть конфликты - разрешить их
Обычно конфликты в:
- `public/index.html` (версия в комментарии)
- `package.json` (версия)

### 3. Только потом пушить
```bash
git push origin develop
```

## 🔄 Автоматическое обновление версии

При каждом коммите **автоматически**:
- ✅ Обновляется версия в `public/index.html`
- ✅ Файл `index.html` добавляется в коммит

**Вы увидите:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 Auto-updating version information...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Version updated to: v5.4.9 (commit: 1bd2bb0)
✅ Version updated in index.html

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  ВАЖНО: Перед push проверьте актуальность версии!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 Если параллельно работают несколько агентов:
   1. Перед push: git pull origin develop
   2. Разрешите конфликты (если есть)
   3. Затем: git push origin develop

📝 После push на Railway деплой займет 2-3 минуты
```

## 📦 Обновление версии вручную

### Только если нужно поднять версию в package.json:

```bash
# Patch: 5.4.9 → 5.4.10
npm run version:patch

# Minor: 5.4.9 → 5.5.0
npm run version:minor

# Major: 5.4.9 → 6.0.0
npm run version:major
```

## ⏱️ Время деплоя

После `git push origin develop`:
- **Railway деплой**: 2-3 минуты
- **Проверка**: https://lexybooster.com/

## 🧪 Тестирование и проверка изменений

**Пользователь ВСЕГДА тестирует в режиме инкогнито:**

```
Chrome: Ctrl + Shift + N
Firefox: Ctrl + Shift + P
Edge: Ctrl + Shift + N
```

**Важно для агентов:**
- Если пользователь говорит "не работает" или "вижу старую версию" - это НЕ проблема кеша
- Пользователь тестирует только в инкогнито (без кеша)
- Все скриншоты делаются в инкогнито
- Значит проблема реальная, а не кеш браузера

**После деплоя:**
1. Подождите 2-3 минуты для завершения деплоя
2. Пользователь откроет https://lexybooster.com/ в инкогнито
3. Проверит версию в правом нижнем углу
4. Протестирует функциональность
5. Сделает скриншоты (если нужно)

## 🚨 Если конфликт версий

При параллельной работе нескольких агентов:

```
<<<<<<< HEAD
<!-- Version: v5.4.9 -->
=======
<!-- Version: v5.4.10 -->
>>>>>>> origin/develop
```

**Решение:**
1. Выбрать бОльшую версию
2. Или `npm run version:patch` для создания новой

## 📚 Полная документация

См. [AUTO_VERSION_UPDATE.md](AUTO_VERSION_UPDATE.md) для деталей.
