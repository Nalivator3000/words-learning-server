# Автоматическое обновление версии

## 🤖 Правила для агентов

**Агенты работают автономно:**
- ✅ Не запрашивайте разрешения на действия, соответствующие контексту
- ✅ Если задача ясна - действуйте без одобрения пользователя
- ✅ Не спрашивайте "Можно ли...", "Должен ли я..."
- ⚠️ Исключение: выбор между кардинально разными архитектурными решениями

## Что настроено

### 1. Pre-commit Hook
Автоматически обновляет версию в `public/index.html` перед каждым коммитом.

**Файл:** `.git/hooks/pre-commit`

**Что делает:**
- Запускает `scripts/utils/update-version.js`
- Обновляет версию в комментарии `<!-- Version: v5.4.9 -->`
- Добавляет обновленный `index.html` в коммит
- Напоминает, что деплой на Railway займет 2-3 минуты

### 2. Update Version Script
Скрипт для автоматического обновления версии.

**Файл:** `scripts/utils/update-version.js`

**Что делает:**
- Читает версию из `package.json`
- Получает текущий git commit hash
- Обновляет версию в `public/index.html`

## Как это работает

### При каждом коммите:

```bash
git add .
git commit -m "feat: New feature"
```

**Автоматически происходит:**
1. ✅ Обновляется версия в `index.html` (из `package.json`)
2. ✅ Файл `index.html` добавляется в коммит
3. ✅ Выводится напоминание о времени деплоя на Railway

### Пример вывода:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 Auto-updating version information...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 Updating version...
✅ Version updated to: v5.4.9 (commit: 1bd2bb0)
✨ Version update complete!
✅ Version updated in index.html

📝 ВАЖНО: После push на Railway деплой займет 2-3 минуты
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Обновление версии вручную

### Patch версия (5.4.9 → 5.4.10)
```bash
npm run version:patch
```

### Minor версия (5.4.9 → 5.5.0)
```bash
npm run version:minor
```

### Major версия (5.4.9 → 6.0.0)
```bash
npm run version:major
```

## Где отображается версия

### В интерфейсе
Версия отображается в правом нижнем углу:

```
App: 5.4.9
SW: none
CSS: v5.3.8
```

### В HTML комментарии
```html
<!-- Version: v5.4.9 -->
```

## Проверка работы

### Тест скрипта обновления:
```bash
node scripts/utils/update-version.js
```

### Проверка версии в index.html:
```bash
grep "<!-- Version:" public/index.html
```

### Проверка наличия hook:
```bash
ls -la .git/hooks/pre-commit
```

## Отключение автообновления

Если нужно временно отключить автообновление версии:

```bash
# Удалить или переименовать hook
mv .git/hooks/pre-commit .git/hooks/pre-commit.disabled
```

Чтобы включить обратно:
```bash
mv .git/hooks/pre-commit.disabled .git/hooks/pre-commit
```

## Важные замечания

1. **Версия берется из package.json** - обновляйте версию через `npm version` команды
2. **Hook работает только локально** - каждый разработчик должен иметь его в своем `.git/hooks/`
3. **Деплой на Railway** - после push изменений деплой займет 2-3 минуты
4. **Версия в UI** - обновляется автоматически при загрузке страницы из `package.json` через API
5. **Тестирование ТОЛЬКО в инкогнито** - все проверки и скриншоты делаются без кеша браузера

## ⚠️ ВАЖНО: Работа с несколькими агентами

Если параллельно работают несколько агентов Claude, **ОБЯЗАТЕЛЬНО** перед каждым push:

### Процедура перед push:

```bash
# 1. Убедитесь, что у вас последняя версия
git pull origin develop

# 2. Если есть конфликты - разрешите их
# Обычно конфликты возникают в:
# - public/index.html (версия)
# - package.json (версия)

# 3. После разрешения конфликтов
git add .
git commit -m "merge: Resolve conflicts"

# 4. Теперь можно пушить
git push origin develop
```

### Почему это важно?

- Каждый коммит обновляет версию в `index.html`
- Если два агента работают параллельно, может возникнуть конфликт версий
- `git pull` перед push гарантирует, что у вас актуальная версия
- Это предотвращает конфликты и неудачные push

### Типичный конфликт:

```
<<<<<<< HEAD
<!-- Version: v5.4.9 -->
=======
<!-- Version: v5.4.10 -->
>>>>>>> origin/develop
```

**Решение:** Выберите бОльшую версию или используйте `npm run version:patch` для создания новой.

## 🧪 Тестирование изменений

### Пользователь ВСЕГДА тестирует в инкогнито

**Важно для агентов:**
- Пользователь делает ВСЕ проверки и скриншоты в режиме инкогнито
- Если пользователь сообщает о проблеме - это НЕ кеш браузера
- Он уже тестирует без кеша, проблема реальная

**Открыть инкогнито:**
- **Chrome/Edge**: `Ctrl + Shift + N`
- **Firefox**: `Ctrl + Shift + P`
- **Safari**: `Cmd + Shift + N`

### Почему инкогнито?

1. **Нет кеша браузера** - всегда свежие файлы с сервера
2. **Чистое состояние** - нет сохраненных данных, cookies
3. **Объективная проверка** - как увидит новый пользователь
4. **Все скриншоты** - делаются только в инкогнито

### Процедура тестирования:

```bash
# 1. Деплой на Railway
git push origin develop

# 2. Ждем 2-3 минуты

# 3. Открываем в инкогнито
# Chrome: Ctrl + Shift + N
# Переходим: https://lexybooster.com/

# 4. Проверяем версию
# Смотрим в правый нижний угол: App: 5.4.10

# 5. Тестируем функциональность
# Проверяем изменения, делаем скриншоты

# 6. Закрываем инкогнито
# Готово!
```

### Что проверять:

- ✅ Версия в UI обновилась
- ✅ Новые функции работают
- ✅ Старые функции не сломались
- ✅ UI отображается корректно
- ✅ Нет ошибок в консоли

## Структура файлов

```
.git/hooks/
  └── pre-commit           # Git hook для автообновления

scripts/utils/
  └── update-version.js    # Скрипт обновления версии

public/
  └── index.html           # Содержит версию в комментарии

package.json               # Источник версии
```
