# Проверка деплоя Google Drive кеширования

## Что было сделано

✅ Запушен код с Google Drive кешированием в `main`
✅ Railway автоматически начал деплой
✅ Переменные окружения настроены:
- `GOOGLE_DRIVE_CREDENTIALS_JSON` ✓
- `GOOGLE_DRIVE_FOLDER_ID` ✓

## Ожидаемое время деплоя

⏱️ **3-5 минут** с момента пуша (только что запушено)

## Как проверить, что деплой завершён

### 1. Проверка API статистики кэша

```bash
curl https://words-learning-server-production.up.railway.app/api/tts/cache/stats
```

**До деплоя (старый формат):**
```json
{
  "cached_items": 0,
  "total_size_bytes": 0,
  "total_size_mb": "0.00"
}
```

**После деплоя (новый формат с Google Drive):**
```json
{
  "local": {
    "cached_items": 0,
    "total_size_bytes": 0,
    "total_size_mb": "0.00"
  },
  "google_drive": {
    "enabled": true,
    "cached_items": 0,
    "total_size_mb": "0.00"
  }
}
```

### 2. Проверка логов Railway

1. Откройте Railway Dashboard: https://railway.app/
2. Перейдите в проект `words-learning-server`
3. Откройте вкладку **Deployments**
4. Кликните на последний деплой (должен быть статус **Active**)
5. Откройте **View Logs**

**Ищите строку:**
```
✅ Google Drive cache initialized successfully
```

### 3. Проверка версии приложения

Откройте https://lexybooster.com и посмотрите в консоли браузера (F12):

```
Build version: 5.3.0 (или выше)
```

Старая версия: `5.2.7` или `5.2.9`

## Проверка работы Google Drive кеширования

После завершения деплоя:

### Шаг 1: Протестируйте TTS

1. Откройте https://lexybooster.com
2. Войдите в аккаунт (en→de)
3. Запустите квиз
4. Нажмите на кнопку озвучки 🔊 для нескольких немецких слов

### Шаг 2: Проверьте логи Railway

В логах должны появиться:

**При первом запросе (генерация):**
```
🔊 TTS Request: "der Hund" (de-DE)
💾 Generating audio: "der Hund"
✅ Generated audio for "der Hund": 12345 bytes
☁️ Uploaded to Google Drive: der_Hund-de-DE.mp3 (ID: abc123...)
```

**При повторном запросе (кэш):**
```
🔊 TTS Request: "der Hund" (de-DE)
☁️ Serving from Google Drive cache: "der Hund"
```

### Шаг 3: Проверьте Google Drive папку

1. Откройте папку: https://drive.google.com/drive/folders/1Knq7vVE_l8vphiQoZxUuLQ20eUU46ICx
2. Должны появиться файлы вида: `abc123def456-de-DE.mp3`

**Формат имени файла:**
```
[MD5-хеш]-[язык].mp3
```

Пример:
```
5d41402abc4b2a76b9719d911017c592-de-DE.mp3  (хеш от "der Hund")
```

### Шаг 4: Проверьте статистику кэша

```bash
curl https://words-learning-server-production.up.railway.app/api/tts/cache/stats
```

После озвучки нескольких слов:

```json
{
  "local": {
    "cached_items": 5,
    "total_size_bytes": 123456,
    "total_size_mb": "0.12"
  },
  "google_drive": {
    "enabled": true,
    "cached_items": 5,
    "total_size_mb": "0.12"
  }
}
```

## Если что-то не работает

### Проблема 1: API всё ещё возвращает старый формат

**Решение:**
- Подождите ещё 2-3 минуты
- Проверьте статус деплоя в Railway (должен быть **Active**)
- Проверьте логи деплоя на ошибки

### Проблема 2: `"google_drive": { "enabled": false }`

**Причины:**
- Переменные окружения не загрузились
- JSON в `GOOGLE_DRIVE_CREDENTIALS_JSON` невалиден
- Service account не имеет прав на папку

**Решение:**
1. Проверьте переменные в Railway → Variables
2. Убедитесь, что JSON валиден (без лишних пробелов)
3. Проверьте Share настройки папки

### Проблема 3: Файлы не появляются на Google Drive

**Проверьте логи Railway:**

❌ Если видите ошибки загрузки:
```
❌ Failed to upload to Google Drive: [error message]
```

**Возможные причины:**
- Service account не добавлен в Share папки
- Неправильный FOLDER_ID
- Проблемы с правами доступа

**Решение:**
1. Откройте папку: https://drive.google.com/drive/folders/1Knq7vVE_l8vphiQoZxUuLQ20eUU46ICx
2. Нажмите Share
3. Убедитесь, что `lexybooster-tts-cache@lexybooster-tts.iam.gserviceaccount.com` есть с правами **Editor**
4. Передеплойте Railway (Deployments → Redeploy)

## Дополнительные возможности

### Admin UI для прелоадинга

После деплоя доступна админка:

```
https://words-learning-server-production.up.railway.app/admin-audio-preload.html
```

Функции:
- 💰 Estimate Cost - оценка стоимости прелоадинга
- 📊 Cache Statistics - статистика кэша
- 🎵 Preload Popular Words - автоматическая генерация аудио для популярных слов

### Ручной прелоадинг

```bash
# Оценка
curl "https://words-learning-server-production.up.railway.app/api/tts/preload/estimate?maxWords=100"

# Запуск прелоадинга
curl -X POST https://words-learning-server-production.up.railway.app/api/tts/preload \
  -H "Content-Type: application/json" \
  -d '{"maxWords": 100, "dryRun": false}'
```

## Итоговый чеклист

После деплоя проверьте:

- [ ] API `/api/tts/cache/stats` возвращает новый формат с `google_drive`
- [ ] `"google_drive": { "enabled": true }`
- [ ] Логи Railway показывают "✅ Google Drive cache initialized successfully"
- [ ] После озвучки слов файлы появляются на Google Drive
- [ ] Повторная озвучка тех же слов берётся из кэша (логи показывают "☁️ Serving from Google Drive cache")
- [ ] Админка доступна по адресу `/admin-audio-preload.html`

Если все пункты ✅ - Google Drive кеширование работает полностью! 🎉
