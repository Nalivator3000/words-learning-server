# Cloudinary TTS Audio Cache Setup

## Почему Cloudinary вместо Google Drive?

**Проблема с Google Drive:**
- Service Account не имеет собственной квоты хранилища
- Требует Google Workspace (платно) для Shared Drives
- Сложная настройка OAuth delegation

**Преимущества Cloudinary:**
- ✅ **Бесплатно**: 25 GB хранилища + 25 GB bandwidth/месяц
- ✅ **Простая интеграция**: один API ключ
- ✅ **Быстрый CDN**: файлы раздаются через глобальный CDN
- ✅ **Автоматические трансформации**: можно изменять битрейт, формат на лету
- ✅ **Навсегда бесплатно**: не требует кредитной карты

## Шаг 1: Регистрация в Cloudinary

1. Откройте https://cloudinary.com/users/register_free
2. Зарегистрируйтесь (можно через Google):
   - Email
   - Password
   - Или "Sign up with Google"

3. После регистрации откроется Dashboard

## Шаг 2: Получение API ключей

На Dashboard вы увидите секцию **Account Details**:

```
Cloud name: your-cloud-name
API Key: 123456789012345
API Secret: abcdefghijklmnopqrstuvwxyz1234
```

**Важно**: Скопируйте все три значения!

## Шаг 3: Настройка папки для аудио

1. В Dashboard слева выберите **Media Library**
2. Создайте папку `tts-audio` (или любое другое имя)
3. Скопируйте имя папки

## Шаг 4: Добавление переменных в Railway

1. Откройте Railway Dashboard: https://railway.app/
2. Выберите проект `words-learning-server`
3. Перейдите на вкладку **Variables**
4. Нажмите **+ New Variable**

Добавьте три переменные:

### CLOUDINARY_CLOUD_NAME
```
your-cloud-name
```

### CLOUDINARY_API_KEY
```
123456789012345
```

### CLOUDINARY_API_SECRET
```
abcdefghijklmnopqrstuvwxyz1234
```

### CLOUDINARY_FOLDER (опционально)
```
tts-audio
```

Если не указать, файлы будут в корне.

## Шаг 5: Установка npm пакета

Локально (для разработки):

```bash
npm install cloudinary
```

Railway установит автоматически при деплое.

## Шаг 6: Настройка локального .env

Добавьте в `.env`:

```env
# Cloudinary TTS Cache
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=123456789012345
CLOUDINARY_API_SECRET=abcdefghijklmnopqrstuvwxyz1234
CLOUDINARY_FOLDER=tts-audio
```

## Шаг 7: Проверка настройки

После добавления переменных Railway автоматически передеплоит.

### Проверка 1: API статус

```bash
curl https://lexybooster.com/api/tts/cache/stats
```

**Ожидаемый ответ:**
```json
{
  "local": {
    "cached_items": 0,
    "total_size_bytes": 0,
    "total_size_mb": "0.00"
  },
  "cloudinary": {
    "enabled": true,
    "cached_items": 0,
    "total_size_mb": "0.00"
  }
}
```

### Проверка 2: Генерация аудио

1. Откройте https://lexybooster.com
2. Войдите в аккаунт (en→de)
3. Запустите квиз
4. Озвучьте несколько немецких слов 🔊

### Проверка 3: Cloudinary Media Library

1. Откройте Cloudinary Dashboard → Media Library
2. Откройте папку `tts-audio`
3. Должны появиться `.mp3` файлы

**Формат имени файла:**
```
tts-audio/abc123def456-de-DE
```

## Локальное тестирование

Запустите тестовый скрипт:

```bash
node test-cloudinary-cache.js
```

**Ожидаемый вывод:**
```
🧪 Testing Cloudinary Cache Integration

1️⃣ Initializing Cloudinary cache...
✅ Cloudinary cache initialized successfully

2️⃣ Creating test audio data...
   Test text: "test-word-1704380000000"
   Cache key: abc123...
   Audio size: 50 bytes

3️⃣ Uploading test audio to Cloudinary...
   ☁️ Uploaded to Cloudinary: test-word-... (URL: https://...)
✅ Upload successful

4️⃣ Retrieving test audio from Cloudinary...
✅ Retrieved 50 bytes

5️⃣ Verifying content integrity...
✅ Content matches perfectly

6️⃣ Getting cache statistics...
📊 Cloudinary Cache Stats:
   Enabled: true
   Files: 1
   Size: 0.00 MB

7️⃣ Cleaning up test file...
✅ Cleanup complete

🎉 All tests passed! Cloudinary cache is working correctly.
```

## Как это работает

### 3-уровневое кеширование:

1. **Локальный кэш (Railway volume)** - самый быстрый
   - Проверяется первым
   - Хранится в `audio-cache/` папке
   - Сбрасывается при редеплое

2. **Cloudinary CDN** - персистентный
   - Проверяется вторым
   - Хранится навсегда (или пока не удалишь)
   - Раздаётся через глобальный CDN

3. **Google TTS API** - генерация
   - Используется только если аудио нет в кэше
   - Результат сохраняется в оба кэша

### Поток данных:

```
TTS Request
    ↓
Локальный кэш?
    ↓ нет
Cloudinary кэш?
    ↓ нет
Google TTS генерация
    ↓
Сохранить в локальный кэш
    ↓
Загрузить в Cloudinary (async)
    ↓
Вернуть клиенту
```

## API Endpoints

### GET /api/tts/cache/stats

Статистика кэша:

```bash
curl https://lexybooster.com/api/tts/cache/stats
```

### DELETE /api/tts/cache/clear?location=local

Очистить только локальный кэш:

```bash
curl -X DELETE "https://lexybooster.com/api/tts/cache/clear?location=local"
```

### DELETE /api/tts/cache/clear?location=cloudinary

Очистить только Cloudinary:

```bash
curl -X DELETE "https://lexybooster.com/api/tts/cache/clear?location=cloudinary"
```

### DELETE /api/tts/cache/clear?location=all

Очистить всё:

```bash
curl -X DELETE "https://lexybooster.com/api/tts/cache/clear?location=all"
```

## Преимущества для пользователей

### Скорость:
- **Первый запрос**: 500-1000ms (генерация через Google TTS)
- **Повторный запрос (локальный кэш)**: 5-10ms
- **Повторный запрос (Cloudinary)**: 50-100ms
- **Повторный запрос (после редеплоя)**: 50-100ms (из Cloudinary)

### Экономия:
- Бесплатный лимит Google TTS: **1 млн символов/месяц**
- Среднее слово: **~15 символов**
- Без кэша: **66,666 слов/месяц бесплатно**
- С кэшем: **неограниченно** (один раз сгенерированное аудио используется вечно)

### Надёжность:
- Если Cloudinary недоступен → используется локальный кэш
- Если локальный кэш пуст → запрос к Cloudinary
- Если Cloudinary пуст → генерация через Google TTS

## Мониторинг использования

### Cloudinary Dashboard

1. Откройте https://cloudinary.com/console
2. **Usage** → смотрите:
   - Storage used: сколько занято места
   - Bandwidth: сколько трафика использовано
   - Transformations: количество запросов

### Бесплатный план Cloudinary:

- Storage: **25 GB**
- Bandwidth: **25 GB/месяц**
- Transformations: **25,000/месяц**

### Примерный расчёт:

- Среднее аудио: **~10 KB**
- 25 GB = **2.5 млн аудио файлов**
- Bandwidth 25 GB/месяц = **2.5 млн проигрываний/месяц**

**Вывод**: Бесплатного плана хватит надолго! 🎉

## Автоматический прелоадинг

После настройки Cloudinary можно использовать автоматический прелоадинг популярных слов:

```bash
# Оценка стоимости
curl "https://lexybooster.com/api/tts/preload/estimate?maxWords=500"

# Запуск прелоадинга
curl -X POST https://lexybooster.com/api/tts/preload \
  -H "Content-Type: application/json" \
  -d '{"maxWords": 500, "dryRun": false}'
```

Все сгенерированные файлы автоматически загрузятся в Cloudinary.

## Миграция с Google Drive на Cloudinary

Если у вас уже были файлы на Google Drive, не переживайте:

1. Старый код с Google Drive удалён
2. Новый код использует Cloudinary
3. При первом запросе к старому слову аудио будет:
   - Сгенерировано заново (бесплатно, если в лимите)
   - Сохранено в Cloudinary
   - В следующий раз будет браться из Cloudinary

Никакой ручной миграции не требуется - всё произойдёт автоматически при использовании приложения.

## Troubleshooting

### Проблема: `"cloudinary": { "enabled": false }`

**Причины:**
- Переменные окружения не установлены
- Неправильный API ключ

**Решение:**
1. Проверьте переменные в Railway → Variables
2. Убедитесь, что все три переменные установлены:
   - `CLOUDINARY_CLOUD_NAME`
   - `CLOUDINARY_API_KEY`
   - `CLOUDINARY_API_SECRET`
3. Передеплойте Railway

### Проблема: Файлы не появляются в Cloudinary

**Проверьте логи Railway:**

Должны быть строки:
```
☁️ Uploaded to Cloudinary: abc123-de-DE.mp3
```

Если есть ошибки:
```
❌ Failed to upload to Cloudinary: [error message]
```

**Возможные причины:**
- Неправильный API Secret
- Превышена квота (маловероятно на бесплатном плане)
- Проблемы с сетью

**Решение:**
1. Проверьте правильность всех трёх ключей
2. Убедитесь, что API Secret скопирован полностью
3. Проверьте квоту в Cloudinary Dashboard

### Проблема: "Invalid API key or secret"

**Решение:**
1. Перейдите в Cloudinary Dashboard → Settings → Security
2. Убедитесь, что используете **API Secret** (не путайте с API Key!)
3. При необходимости сгенерируйте новый API Secret:
   - Settings → Security → API Keys
   - Revoke & Generate New Secret

### Проблема: Медленная загрузка аудио

**Проверьте:**
1. Логи - файл берётся из Cloudinary или генерируется заново?
2. Если генерируется заново каждый раз - проблема с именем файла (cache key)

**Решение:**
- Очистите локальный кэш
- Проверьте Cloudinary Media Library - там должны быть файлы
- Если файлов нет - проверьте логи Railway на ошибки загрузки

## Безопасность

### API Secret

**ВАЖНО**: Никогда не коммитьте `CLOUDINARY_API_SECRET` в git!

Переменная должна быть:
- ✅ В Railway Variables
- ✅ В локальном `.env` (который в `.gitignore`)
- ❌ НЕ в коде
- ❌ НЕ в публичном репозитории

### Защита от злоупотреблений

Cloudinary автоматически защищает от:
- Hotlinking (если включить в настройках)
- DDoS атак
- Превышения квоты

Но рекомендую:
1. Включить **Strict transformations** в Cloudinary Settings
2. Настроить **Rate limiting** в вашем приложении (уже есть)
3. Мониторить квоту раз в месяц

## Итоговый чеклист

После настройки проверьте:

- [ ] Переменные `CLOUDINARY_*` добавлены в Railway
- [ ] Railway передеплоился успешно (статус Active)
- [ ] API `/api/tts/cache/stats` показывает `"cloudinary": { "enabled": true }`
- [ ] После озвучки слов файлы появляются в Cloudinary Media Library
- [ ] Повторная озвучка тех же слов берётся из кэша (быстрее)
- [ ] Локальный тест `node test-cloudinary-cache.js` проходит успешно

Если все пункты ✅ - Cloudinary кеширование работает! 🎉

## Полезные ссылки

- Cloudinary Dashboard: https://cloudinary.com/console
- Media Library: https://cloudinary.com/console/media_library
- Usage Statistics: https://cloudinary.com/console/usage
- API Documentation: https://cloudinary.com/documentation/node_integration
- Support: https://support.cloudinary.com

## Следующие шаги

После настройки Cloudinary:

1. **Протестируйте работу** - озвучьте несколько слов на lexybooster.com
2. **Проверьте Cloudinary** - убедитесь, что файлы загружаются
3. **Настройте прелоадинг** - используйте админку `/admin-audio-preload.html`
4. **Мониторьте квоту** - раз в месяц проверяйте Usage в Cloudinary

Всё готово! 🚀
