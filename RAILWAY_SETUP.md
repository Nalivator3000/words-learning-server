# Railway Deployment Setup

## 🚀 Процессы на Railway

Приложение использует **2 процесса**:

### 1. Web Process (основной сервер)
```bash
node server-postgresql.js
```
- Запускается автоматически при деплое
- Обрабатывает HTTP запросы
- Порт определяется Railway автоматически

### 2. Worker Process (фоновые задачи)
```bash
node scripts/translations/auto-translate-cron.js 6
```
- Запускает переводы каждые 6 часов
- Запускает мониторинг прогресса каждый час
- Сохраняет прогресс в `.translation-progress.json` и `.translation-progress-history.json`

## 📋 Настройка Worker Process на Railway

### Шаг 1: Создать новый Service
1. Откройте проект на [railway.app](https://railway.app)
2. Нажмите **"New Service"** → **"Empty Service"**
3. Назовите: `translation-worker`

### Шаг 2: Подключить к репозиторию
1. В настройках нового сервиса выберите **"Connect to GitHub"**
2. Выберите ваш репозиторий `words-learning-server`
3. Ветка: `develop` (или `main`)

### Шаг 3: Настроить переменные окружения
Скопируйте все переменные окружения из основного web сервиса:
- `DATABASE_URL`
- `NODE_ENV=production`
- Все остальные переменные из `.env`

### Шаг 4: Настроить Start Command
В настройках Worker сервиса:
1. Settings → Deploy
2. **Start Command**: `node scripts/translations/auto-translate-cron.js 6`
3. **Restart Policy**: `ON_FAILURE`
4. **Max Retries**: `10`

### Шаг 5: Deploy
1. Нажмите **"Deploy"**
2. Worker начнет работать автоматически

## 📊 Мониторинг

### Проверка логов Worker процесса
```
Railway Dashboard → Worker Service → Logs
```

Вы увидите:
```
🤖 AUTO TRANSLATION CRON JOB STARTED
⏰ Start time: ...
⏱️  Translation interval: Every 6 hours
📊 Monitor interval: Every hour
```

### Проверка прогресса вручную (локально)
```bash
npm run monitor
```

## 🔄 Workflow

1. **Каждые 6 часов**: Worker запускает перевод новой партии слов (3 пары параллельно)
2. **После каждого перевода**: Автоматически запускается мониторинг
3. **Каждый час**: Запускается мониторинг прогресса
4. **Данные сохраняются**:
   - `.translation-progress.json` - текущий прогресс переводов
   - `.translation-progress-history.json` - история изменений (последние 100 снимков)

## 🛠️ Полезные команды

### Локальный запуск worker (для тестирования)
```bash
node scripts/translations/auto-translate-cron.js 6
```

### Разовый запуск переводов
```bash
npm run translate:matrix
```

### Проверка прогресса
```bash
npm run monitor
```

### Проверка статуса всех переводов
```bash
node check-all-translation-progress.js
```

## 📈 Прогресс переводов

### Phase 1: Complete Big 3 ✅ (12/12 pairs)
- German, English, Spanish → Japanese, Korean, Hindi, Turkish

### Phase 2: European Languages ✅ (39/39 pairs)
- French, Italian, Portuguese → All languages

### Phase 3: Arabic & Chinese ⏳ (3/32 pairs) - **В ПРОЦЕССЕ**
- Arabic → Turkish (54.7% ✅)
- Arabic → Swahili (50.6% ✅)
- Arabic → Chinese (54.2% ✅)

### Phase 4: Russian (0/34 pairs) - Готов к запуску
- Source words созданы ✅ (10,000 слов)

### Phase 5: Ukrainian (0/34 pairs) - Готов к запуску
- Source words созданы ✅ (10,000 слов)

### Phase 6-7: Waiting
- Japanese, Korean, Hindi, Turkish → All

## 🎯 Итого
- **Запланировано**: 219 пар
- **Завершено**: 51 пар (23.3%)
- **В процессе**: 3 пары (Phase 3)
- **Готово к запуску**: 68 пар (Phase 4 + Phase 5)
- **Ожидает**: 97 пар (Phase 6 + Phase 7)

## 📝 Примечания

1. Worker процесс работает **независимо** от web процесса
2. При деплое нового кода оба процесса перезапускаются
3. Прогресс сохраняется в базе данных и JSON файлах
4. Worker автоматически продолжит с места остановки после перезапуска
5. Мониторинг показывает скорость перевода (~6800 слов/час)
