# Railway Deployment Setup

## 🚀 Деплой на Railway

Приложение запускает основной сервер:

```bash
node server-postgresql.js
```

- Запускается автоматически при деплое
- Обрабатывает HTTP запросы
- Порт определяется Railway автоматически

## 🔧 Запуск автоматических переводов

### Вариант 1: Временный запуск worker'а (рекомендуется)

Подключитесь к Railway через CLI и запустите worker в фоновом режиме:

```bash
# Установка Railway CLI (если не установлен)
npm install -g @railway/cli

# Логин в Railway
railway login

# Подключение к проекту
railway link

# Запуск worker в фоновом режиме
railway run node scripts/translations/auto-translate-cron.js 6 &
```

Worker будет работать пока не завершится деплой или вы не остановите его вручную.

### Вариант 2: Локальный запуск с подключением к production БД

```bash
# Установите DATABASE_URL в .env на production значение
# Затем запустите локально:
npm run translate:auto
```

## 📊 Мониторинг

### Проверка прогресса
```bash
npm run monitor
```

Вы увидите:
```
📊 TRANSLATION PROGRESS MONITOR WITH HISTORY
⏳ AR→TR: 54.7% (5470/10000)
⏳ AR→SW: 50.6% (5060/10000)
📈 CHANGES SINCE LAST CHECK
📊 Total words added: 753
⚡ Average rate: 6846 words/hour
```

## 🔄 Как это работает

Когда вы запускаете `npm run translate:auto`:

1. **Каждые 6 часов**: Запускается перевод новой партии слов (3 пары параллельно)
2. **После каждого перевода**: Автоматически запускается мониторинг
3. **Каждый час**: Запускается мониторинг прогресса
4. **Данные сохраняются**:
   - `.translation-progress.json` - текущий прогресс переводов (откуда продолжить)
   - `.translation-progress-history.json` - история изменений (последние 100 снимков)

При перезапуске worker автоматически продолжит с места остановки.

## 🛠️ Полезные команды

### Локальный запуск worker (для тестирования)
```bash
node scripts/translations/auto-translate-cron.js 6
```

### Разовый запуск переводов
```bash
npm run translate:matrix
```

### Проверка прогресса
```bash
npm run monitor
```

### Проверка статуса всех переводов
```bash
node check-all-translation-progress.js
```

## 📈 Прогресс переводов

### Phase 1: Complete Big 3 ✅ (12/12 pairs)
- German, English, Spanish → Japanese, Korean, Hindi, Turkish

### Phase 2: European Languages ✅ (39/39 pairs)
- French, Italian, Portuguese → All languages

### Phase 3: Arabic & Chinese ⏳ (3/32 pairs) - **В ПРОЦЕССЕ**
- Arabic → Turkish (54.7% ✅)
- Arabic → Swahili (50.6% ✅)
- Arabic → Chinese (54.2% ✅)

### Phase 4: Russian (0/34 pairs) - Готов к запуску
- Source words созданы ✅ (10,000 слов)

### Phase 5: Ukrainian (0/34 pairs) - Готов к запуску
- Source words созданы ✅ (10,000 слов)

### Phase 6-7: Waiting
- Japanese, Korean, Hindi, Turkish → All

## 🎯 Итого
- **Запланировано**: 219 пар
- **Завершено**: 51 пар (23.3%)
- **В процессе**: 3 пары (Phase 3)
- **Готово к запуску**: 68 пар (Phase 4 + Phase 5)
- **Ожидает**: 97 пар (Phase 6 + Phase 7)

## 📝 Примечания

1. **Временные файлы**: После завершения переводов можно удалить:
   - `.translation-progress.json`
   - `.translation-progress-history.json`
   - Все файлы вида `*.md` в корне (отчёты)
   - Временные скрипты в корне

2. **Прогресс сохраняется**: Worker автоматически продолжит с места остановки после перезапуска

3. **Скорость перевода**: ~6800 слов/час (можно увеличить, запустив несколько worker'ов параллельно)

4. **Production БД**: Worker работает с той же БД, что и основное приложение
