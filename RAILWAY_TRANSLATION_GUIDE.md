# Railway Translation Execution Guide

## Проблема
Локальное подключение к Railway PostgreSQL блокируется с ошибкой `ECONNRESET`. Решение - запустить скрипты переводов напрямую на Railway, где есть прямой доступ к базе данных.

---

## Вариант 1: Railway Dashboard (Рекомендуется)

### Шаг 1: Откройте Railway Dashboard
1. Перейдите на https://railway.app/
2. Войдите в свой проект `words-learning-server`
3. Выберите ваш сервис (main service)

### Шаг 2: Запустите одноразовую команду
1. Нажмите на вкладку **"Settings"**
2. Прокрутите до **"Custom Start Command"** или используйте **"One-off Commands"**
3. Введите команду:
   ```bash
   npm run translate:matrix
   ```

### Шаг 3: Мониторинг
Следите за логами в реальном времени:
- Откройте вкладку **"Deployments"**
- Выберите последний deployment
- Смотрите логи выполнения

---

## Вариант 2: Railway CLI (Требует установки)

### Установка Railway CLI

**Windows (PowerShell):**
```powershell
iwr https://railway.app/install.ps1 | iex
```

**Или через npm:**
```bash
npm install -g @railway/cli
```

### Использование

```bash
# Логин
railway login

# Подключение к проекту
railway link

# Запуск команды на Railway
railway run npm run translate:matrix

# Или автоматический режим
railway run npm run translate:auto

# Проверка статуса
railway run npm run translate:check
```

---

## Вариант 3: Создать отдельный Worker Service

### Шаг 1: Создайте новый файл `worker-translate.js`
```javascript
#!/usr/bin/env node
require('dotenv').config();
const { spawn } = require('child_process');

console.log('🚀 Translation Worker Started');
console.log('DATABASE_URL:', process.env.DATABASE_URL ? 'Set ✅' : 'Not set ❌');

// Запуск автоматического переводчика
const child = spawn('node', ['scripts/translations/auto-translate-cron.js', '6'], {
    stdio: 'inherit',
    env: process.env
});

child.on('exit', (code) => {
    console.log(`Worker exited with code ${code}`);
    process.exit(code);
});
```

### Шаг 2: Добавьте в package.json
```json
"scripts": {
  "worker": "node worker-translate.js"
}
```

### Шаг 3: Создайте новый Service на Railway
1. В Railway Dashboard нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий `words-learning-server`
3. В настройках сервиса укажите:
   - **Start Command:** `npm run worker`
   - **Environment Variables:** Скопируйте DATABASE_URL из основного сервиса

---

## Рекомендуемый Workflow

### Для быстрого старта (Вариант 1):
```
1. Railway Dashboard → Settings → One-off Command
2. Введите: npm run translate:matrix
3. Следите за прогрессом в логах
```

### Для автоматического режима (Вариант 3):
```
1. Создайте worker service
2. Worker будет запускаться каждые 6 часов автоматически
3. Прогресс сохраняется в .translation-progress.json
```

---

## Мониторинг Progress

### На Railway:
```bash
railway run cat scripts/translations/.translation-progress.json
```

### Или через npm script:
```bash
railway run npm run translate:check
```

---

## Важные Команды

| Команда | Описание |
|---------|----------|
| `npm run translate:matrix` | Запустить параллельный перевод (3 воркера, один батч) |
| `npm run translate:auto` | Запустить автоматический режим (каждые 6ч) |
| `npm run translate:check` | Проверить статус всех переводов |

---

## Что делать после запуска

1. **Проверьте логи** - убедитесь, что скрипт начал работу
2. **Дождитесь первого батча** - обычно 10-20 минут на 3 пары
3. **Проверьте прогресс** - `railway run npm run translate:check`
4. **Продолжайте запускать** - или настройте автоматический worker

---

## Оценка времени выполнения

- **1 пара:** ~7-10 минут
- **1 батч (3 пары):** ~10-20 минут
- **Phase 1 (12 пар):** ~40-80 минут (4 батча)
- **Все 215 пар:** ~25-50 часов (72 батча)

**С автоматическим запуском каждые 6 часов:** ~10-15 дней

---

## Troubleshooting

### Ошибка: ECONNRESET
**Решение:** Именно поэтому нужен запуск на Railway. Локально не работает из-за лимитов подключений.

### Ошибка: Module not found
**Решение:** Убедитесь, что все изменения запушены в GitHub и Railway автоматически задеплоил:
```bash
git push
```

### Скрипт зависает
**Решение:** Проверьте логи Railway на наличие ошибок API или превышения лимитов Google Translate.

---

**Создано:** 2025-12-25
**Статус:** Готово к запуску на Railway 🚀
