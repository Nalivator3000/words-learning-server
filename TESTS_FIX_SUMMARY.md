# Исправление упавших тестов на Railway

## 🔍 Проблема

Большинство E2E тестов падали при запуске против production окружения на Railway с ошибками:

```
TimeoutError: page.waitForSelector: Timeout 10000ms exceeded.
- waiting for locator('#loginTab') to be visible
- waiting for locator('#authModal') to be hidden
```

## 🎯 Причина

Тесты пытались подключиться к **несуществующему URL**: `https://your-app.up.railway.app` вместо реального production URL: **`https://lexybooster.com`**

## ✅ Исправления

### 1. Обновлен production конфиг Playwright

**Файл:** [config/playwright.config.production.js](config/playwright.config.production.js)

```diff
- baseURL: process.env.PRODUCTION_URL || 'https://your-app.up.railway.app',
+ baseURL: process.env.PRODUCTION_URL || 'https://lexybooster.com',
```

### 2. Обновлен test runner для production

**Файл:** [run-tests-production.js](run-tests-production.js)

```diff
- const PRODUCTION_URL = process.env.PRODUCTION_URL || 'https://words-learning-server-production.up.railway.app';
+ const PRODUCTION_URL = process.env.PRODUCTION_URL || 'https://lexybooster.com';
```

### 3. Обновлена документация

**Файл:** [RAILWAY_TESTING_GUIDE.md](RAILWAY_TESTING_GUIDE.md)

- Заменены все примеры с `your-app.up.railway.app` на `lexybooster.com`
- Добавлено примечание, что URL уже настроен по умолчанию
- Упрощены примеры команд

## 🚀 Как запустить тесты теперь

### Критические тесты (рекомендуется)

```bash
node run-tests-production.js critical
```

Это протестирует:
- ✅ Авторизацию всех 39 тестовых пользователей
- ✅ Дедупликацию импорта (самая важная функция!)

**Время выполнения:** 10-15 минут

### Smoke тесты (быстрая проверка)

```bash
node run-tests-production.js smoke
```

**Время выполнения:** 5-7 минут

### Все тесты (полная проверка)

```bash
node run-tests-production.js full
```

**Время выполнения:** 60+ минут

## 📊 Просмотр результатов

После запуска тестов:

```bash
npx playwright show-report config/test-results/production-report
```

Откроется HTML отчет с:
- ✅ Passed tests (зеленые)
- ❌ Failed tests (красные)
- 📸 Screenshots при ошибках
- 📹 Videos при ошибках
- ⏱️ Время выполнения каждого теста

## ⚠️ Важно

### Проверьте наличие тестовых пользователей

Тесты используют 39 тестовых пользователей (test_de_en, test_hi_en, и т.д.) с паролем `test123`.

**Убедитесь, что они существуют на production!**

Если нет, создайте их:

```bash
# Получите DATABASE_URL из Railway dashboard
# Variables → Database → DATABASE_URL

# Запустите скрипт создания пользователей
DATABASE_URL="postgresql://..." node scripts/create-test-users.js
```

### Проверьте доступность сайта

Убедитесь, что https://lexybooster.com доступен:

```bash
curl -I https://lexybooster.com
```

Должен вернуть `200 OK` или `302 Found`.

## 🎯 Ожидаемые результаты

После исправления URL все тесты должны работать корректно:

**Критические тесты:**
- 01-authentication.spec.js: ~25 тестов
- 04-import-deduplication.spec.js: ~30 тестов

**Все должны пройти ✅**

Если тесты все еще падают, проверьте:
1. ✅ Сайт доступен (https://lexybooster.com)
2. ✅ Тестовые пользователи созданы
3. ✅ База данных доступна
4. ✅ Нет ошибок в логах Railway

## 📝 Следующие шаги

1. **Запустите critical tests:**
   ```bash
   node run-tests-production.js critical
   ```

2. **Посмотрите отчет:**
   ```bash
   npx playwright show-report config/test-results/production-report
   ```

3. **Если все прошло ✅ - готово!**

4. **Если есть ошибки ❌:**
   - Проверьте отчет
   - Посмотрите скриншоты ошибок
   - Проверьте логи Railway

---

**Исправлено:** 30 декабря 2025
**Статус:** Готово к тестированию ✅
