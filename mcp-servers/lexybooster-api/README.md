# 🤖 LexyBooster API MCP Server

MCP (Model Context Protocol) сервер для доступа к API LexyBooster из AI агентов.

## 📋 Оглавление

- [Что это?](#что-это)
- [Доступные инструменты](#доступные-инструменты)
- [Тестовые аккаунты](#тестовые-аккаунты)
- [API Эндпоинты](#api-эндпоинты)
- [Примеры использования](#примеры-использования)
- [Настройка](#настройка)

---

## Что это?

Этот MCP сервер предоставляет AI агентам прямой доступ к:

- ✅ **40 тестовым аккаунтам** с разными языковыми парами
- ✅ **Всем API эндпоинтам** LexyBooster (аутентификация, слова, геймификация, аналитика)
- ✅ **Хелперам для тестирования** (логин, получение данных, статистика)
- ✅ **Автоматическому rate limiting** для защиты продакшена

---

## Доступные инструменты

### 1️⃣ `get_test_accounts`

Получить список всех тестовых аккаунтов.

**Параметры:**
- `priority` (опционально): `all`, `high`, `medium`, `low`, `empty`

**Пример:**
```json
{
  "priority": "high"
}
```

**Результат:**
```json
[
  {
    "email": "test.de.en@lexibooster.test",
    "password": "test123",
    "from": "de",
    "to": "en",
    "userId": 50
  },
  ...
]
```

---

### 2️⃣ `list_api_endpoints`

Получить список всех доступных API эндпоинтов.

**Параметры:**
- `category` (опционально): `all`, `auth`, `word_sets`, `gamification`, `analytics`, `language_pairs`, `collections`

**Пример:**
```json
{
  "category": "gamification"
}
```

**Результат:**
```json
{
  "leaderboardGlobal": {
    "method": "GET",
    "path": "/api/gamification/leaderboard/global",
    "public": true
  },
  ...
}
```

---

### 3️⃣ `api_call`

Универсальный вызов любого API эндпоинта.

**Параметры:**
- `method` (обязательно): `GET`, `POST`, `PUT`, `DELETE`
- `path` (обязательно): путь к эндпоинту (например, `/api/word-sets`)
- `data` (опционально): тело запроса для POST/PUT
- `headers` (опционально): дополнительные заголовки

**Пример GET:**
```json
{
  "method": "GET",
  "path": "/api/gamification/leaderboard/global"
}
```

**Пример POST:**
```json
{
  "method": "POST",
  "path": "/api/auth/login",
  "data": {
    "email": "test.de.en@lexibooster.test",
    "password": "test123"
  }
}
```

**Результат:**
```json
{
  "status": 200,
  "headers": {...},
  "body": {...}
}
```

---

### 4️⃣ `login`

Быстрый логин с тестовым аккаунтом.

**Параметры:**
- `email` (обязательно): email тестового аккаунта
- `password` (опционально, по умолчанию: `test123`)

**Пример:**
```json
{
  "email": "test.de.en@lexibooster.test"
}
```

**Результат:**
```json
{
  "status": 200,
  "success": true,
  "user": {
    "id": 50,
    "email": "test.de.en@lexibooster.test",
    "username": "test_de_en",
    ...
  }
}
```

---

### 5️⃣ `get_word_sets`

Получить наборы слов для пользователя.

**Параметры:**
- `userId` (опционально): ID пользователя
- `fromLang` (опционально): исходный язык (например, `de`)
- `toLang` (опционально): целевой язык (например, `en`)

**Пример:**
```json
{
  "userId": 50,
  "fromLang": "de",
  "toLang": "en"
}
```

**Результат:**
```json
{
  "count": 17,
  "wordSets": [
    {
      "id": 1,
      "name": "German A1",
      "wordCount": 1000,
      ...
    },
    ...
  ]
}
```

---

### 6️⃣ `get_leaderboard`

Получить таблицу лидеров.

**Параметры:**
- `type` (обязательно): `global` или `weekly`

**Пример:**
```json
{
  "type": "global"
}
```

**Результат:**
```json
{
  "type": "global",
  "count": 150,
  "leaderboard": [
    {
      "rank": 1,
      "username": "john_doe",
      "totalXp": 15000,
      ...
    },
    ...
  ]
}
```

---

### 7️⃣ `get_user_stats`

Получить комплексную статистику пользователя.

**Параметры:**
- `userId` (обязательно): ID пользователя
- `category` (опционально): `all`, `gamification`, `progress`, `exercise`, `difficult`

**Пример:**
```json
{
  "userId": 50,
  "category": "all"
}
```

**Результат:**
```json
{
  "gamification": {
    "totalXp": 5000,
    "level": 12,
    "achievements": 23,
    ...
  },
  "progress": {
    "wordsLearned": 450,
    "accuracy": 87.5,
    ...
  },
  "exercise": {...},
  "difficult": {...}
}
```

---

## Тестовые аккаунты

### 🔥 HIGH PRIORITY (4 аккаунта)

Основные аккаунты для тестирования всех функций.

| Email | Языковая пара | User ID | Особенности |
|-------|---------------|---------|-------------|
| test.de.en@lexibooster.test | Немецкий → Английский | 50 | 17 наборов, темы, CEFR уровни |
| test.de.ru@lexibooster.test | Немецкий → Русский | 51 | 17 наборов, темы, кириллица |
| test.en.ru@lexibooster.test | Английский → Русский | 60 | 6 наборов, кириллица |
| test.en.de@lexibooster.test | Английский → Немецкий | 61 | 6 наборов |

### ⚡ MEDIUM PRIORITY (5 аккаунтов)

Дополнительные языковые пары.

| Email | Языковая пара | User ID |
|-------|---------------|---------|
| test.de.es@lexibooster.test | Немецкий → Испанский | 52 |
| test.de.fr@lexibooster.test | Немецкий → Французский | 53 |
| test.en.es@lexibooster.test | Английский → Испанский | 62 |
| test.hi.en@lexibooster.test | Хинди → Английский | 87 |
| test.hi.de@lexibooster.test | Хинди → Немецкий | 88 |

### 🔍 LOW PRIORITY (2 аккаунта)

Специальные случаи (RTL, иероглифы).

| Email | Языковая пара | User ID | Особенности |
|-------|---------------|---------|-------------|
| test.ar.en@lexibooster.test | Арабский → Английский | 81 | ⚠️ RTL (справа налево) |
| test.zh.en@lexibooster.test | Китайский → Английский | 83 | 🈶 Иероглифы |

### 🗑️ EMPTY (2 аккаунта)

Аккаунты без наборов слов (пустое состояние).

| Email | Языковая пара | User ID |
|-------|---------------|---------|
| test.ru.en@lexibooster.test | Русский → Английский | 85 |
| test.ru.de@lexibooster.test | Русский → Немецкий | 86 |

**Пароль для ВСЕХ аккаунтов:** `test123`

---

## API Эндпоинты

### 🔐 Аутентификация (`/api/auth/*`)

| Эндпоинт | Метод | Описание | Публичный |
|----------|-------|----------|-----------|
| `/api/auth/login` | POST | Вход в систему | ✅ |
| `/api/auth/register` | POST | Регистрация | ✅ |
| `/api/auth/logout` | POST | Выход | ❌ |
| `/api/auth/user` | GET | Получить данные текущего пользователя | ❌ |
| `/auth/google` | GET | OAuth через Google | ✅ |

### 📚 Наборы слов (`/api/word-sets/*`)

| Эндпоинт | Метод | Описание | Публичный |
|----------|-------|----------|-----------|
| `/api/word-sets` | GET | Список всех наборов слов | ✅ |
| `/api/word-sets/:setId` | GET | Получить конкретный набор | ✅ |
| `/api/word-sets` | POST | Создать набор | ❌ |
| `/api/word-sets/:setId/import` | POST | Импортировать набор | ❌ |
| `/api/word-sets/:setId/preview` | GET | Предпросмотр набора | ✅ |
| `/api/word-sets/previews/batch` | POST | Batch предпросмотр | ✅ |
| `/api/word-sets/:setId` | DELETE | Удалить набор | ❌ |

### 🎮 Геймификация (`/api/gamification/*`)

| Эндпоинт | Метод | Описание | Публичный |
|----------|-------|----------|-----------|
| `/api/gamification/leaderboard/global` | GET | Глобальная таблица лидеров | ✅ |
| `/api/gamification/leaderboard/weekly` | GET | Недельная таблица лидеров | ✅ |
| `/api/gamification/achievements` | GET | Список всех ачивок | ✅ |
| `/api/gamification/stats/:userId` | GET | Статистика пользователя | ❌ |
| `/api/gamification/xp-log/:userId` | GET | История XP | ❌ |
| `/api/gamification/activity/:userId` | GET | Активность пользователя | ❌ |
| `/api/gamification/achievements/:userId` | GET | Ачивки пользователя | ❌ |
| `/api/gamification/daily-goals/:userId` | GET | Дневные цели | ❌ |

### 📊 Аналитика (`/api/analytics/*`)

| Эндпоинт | Метод | Описание | Публичный |
|----------|-------|----------|-----------|
| `/api/analytics/progress/:userId` | GET | Прогресс обучения | ❌ |
| `/api/analytics/exercise-stats/:userId` | GET | Статистика упражнений | ❌ |
| `/api/analytics/difficult-words/:userId` | GET | Сложные слова | ❌ |
| `/api/analytics/study-time/:userId` | GET | Время обучения | ❌ |

### 🌍 Языковые пары (`/api/users/:userId/language-pairs/*`)

| Эндпоинт | Метод | Описание | Публичный |
|----------|-------|----------|-----------|
| `/api/users/:userId/language-pairs` | GET | Список языковых пар | ❌ |
| `/api/users/:userId/language-pairs` | POST | Создать пару | ❌ |
| `/api/users/:userId/language-pairs/:pairId/activate` | PUT | Активировать пару | ❌ |
| `/api/users/:userId/language-pairs/:pairId/word-count` | GET | Количество слов | ❌ |
| `/api/users/:userId/language-pairs/:pairId` | DELETE | Удалить пару | ❌ |

### 📦 Коллекции (`/api/*-collections/*`)

| Эндпоинт | Метод | Описание | Публичный |
|----------|-------|----------|-----------|
| `/api/global-collections` | GET | Глобальные коллекции | ✅ |
| `/api/global-collections/:id` | GET | Конкретная коллекция | ✅ |
| `/api/global-collections/:id/import` | POST | Импортировать коллекцию | ❌ |
| `/api/universal-collections` | GET | Универсальные коллекции | ✅ |
| `/api/universal-collections/:id` | GET | Конкретная коллекция | ✅ |
| `/api/universal-collections/:id/import` | POST | Импортировать коллекцию | ❌ |

---

## Примеры использования

### Пример 1: Получить все тестовые аккаунты

```javascript
// Вызов инструмента get_test_accounts
{
  "name": "get_test_accounts",
  "arguments": {
    "priority": "all"
  }
}
```

### Пример 2: Залогиниться и получить данные пользователя

```javascript
// Шаг 1: Логин
{
  "name": "login",
  "arguments": {
    "email": "test.de.en@lexibooster.test"
  }
}

// Шаг 2: Получить наборы слов
{
  "name": "get_word_sets",
  "arguments": {
    "userId": 50,
    "fromLang": "de",
    "toLang": "en"
  }
}
```

### Пример 3: Получить полную статистику пользователя

```javascript
{
  "name": "get_user_stats",
  "arguments": {
    "userId": 50,
    "category": "all"
  }
}
```

### Пример 4: Проверить таблицу лидеров

```javascript
{
  "name": "get_leaderboard",
  "arguments": {
    "type": "global"
  }
}
```

### Пример 5: Прямой вызов API

```javascript
// Получить ачивки
{
  "name": "api_call",
  "arguments": {
    "method": "GET",
    "path": "/api/gamification/achievements"
  }
}

// Создать набор слов (требует аутентификации)
{
  "name": "api_call",
  "arguments": {
    "method": "POST",
    "path": "/api/word-sets",
    "data": {
      "name": "My Custom Set",
      "fromLang": "de",
      "toLang": "en"
    },
    "headers": {
      "Authorization": "Bearer <token>"
    }
  }
}
```

---

## Настройка

### Переменные окружения

```bash
# URL базового API (по умолчанию: https://lexybooster.com)
LEXYBOOSTER_BASE_URL=https://lexybooster.com
```

### Установка в Claude

1. Добавить в `.claude/mcp-config.json`:

```json
{
  "mcpServers": {
    "lexybooster-api": {
      "command": "node",
      "args": ["mcp-servers/lexybooster-api/index.js"],
      "env": {
        "LEXYBOOSTER_BASE_URL": "https://lexybooster.com"
      }
    }
  }
}
```

2. Перезапустить Claude Code

3. Проверить доступность инструментов:
   - `get_test_accounts`
   - `list_api_endpoints`
   - `api_call`
   - `login`
   - `get_word_sets`
   - `get_leaderboard`
   - `get_user_stats`

---

## Rate Limiting

⚠️ **Важно:** Все запросы автоматически соблюдают rate limiting:

- **1000 мс** задержка между запросами
- Автоматический retry при получении 429 (Too Many Requests)
- IP тестирования whitelisted для E2E тестов: `176.199.209.166`

---

## Логирование

Все вызовы API логируются в:
- `console.error` для ошибок
- `console.log` для успешных запросов (в режиме DEBUG)

Для включения подробного логирования:
```bash
DEBUG=true node mcp-servers/lexybooster-api/index.js
```

---

## Troubleshooting

### Проблема: Connection refused

**Решение:** Проверьте, что BASE_URL правильный и сервер доступен:
```bash
curl https://lexybooster.com/api/word-sets
```

### Проблема: 429 Too Many Requests

**Решение:** MCP сервер автоматически соблюдает rate limiting. Подождите 1 секунду между запросами.

### Проблема: 401 Unauthorized

**Решение:** Некоторые эндпоинты требуют аутентификации. Используйте инструмент `login` сначала.

---

## Лицензия

MIT

---

**Создано:** 2026-01-06
**Версия:** 1.0.0
**Автор:** Claude Code AI Agent
